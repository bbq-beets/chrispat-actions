
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    
    - run: echo Hello World change again
    
    - uses: ./hello-world-action
      with:
        who-to-greet: 'John Doe'
      id: hello
      
    - run: echo "The time was ${{ actions.hello.outputs.time }}"
    
   
