on: [push]

jobs:
  build:
    runs-on:
      pool: default
    actions:
    - uses: ./webapp-deploy
      with: 
        app-name: linuxwarapp
        package: "C:\\Users\\suaggar\\Downloads\\ROOT.war"