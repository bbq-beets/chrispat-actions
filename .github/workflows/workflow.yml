on:
  push:
    branches:
    - master
  pull_request:
    branches:
    - master
   
jobs:
  build:
    runs-on: ubuntu-latest
    actions:
    - run: echo Hello World change
    - uses: ./hello-world-action
      with:
        who-to-greet: 'Mundo'