on: [push]

jobs:
  build:
    runs-on:
      pool: default
    actions:
    - uses: ./webapp-deploy
      with: 
        app-name: suaggarTest1
        package: "C:\\Users\\suaggar\\Downloads\\ROOT.war"