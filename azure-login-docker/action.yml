name: 'Azure login'
description: 'Login to Azure'
inputs:
  greeting:
    default: '${{ secrets.TEST_SECRET }}'
runs:
  using: 'docker'
  image: 'Dockerfile' 
  args: ['${{ inputs.greeting }}']
  env:
    TEST: '${{ secrets.TEST_SECRET }}'